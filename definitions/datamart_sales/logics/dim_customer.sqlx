-- Generated by Tiger Data Fabric (Dataform edition)
-- Target: gcp-accelerator-380712.gold_sales.dim_customer
-- Tags: datamart_sales, dim_customer, gold, daily
-- Idempotent via ${ref()} + Dataform incremental/operations semantics
config {
  type: "table",
  schema: "gold_sales",
  tags: ["datamart_sales", "dim_customer", "gold", "daily"],
  bigquery: {
    clusterBy: ["customer_id", "country"],
  },
  assertions: {
    uniqueKey: ["customer_id"]
  },
  description: "Customer dimension."
}

SELECT
  customer_data_v1.customer_id AS customer_id,
  customer_data_v1.customer_name AS customer_name,
  customer_data_v1.country AS country,
  customer_data_v1.signup_ts AS signup_ts
FROM ${ref("customer_data_v1")} AS customer_data_v1
WHERE is_active = TRUE


post_operations {
  ALTER TABLE ${self()}
  ADD PRIMARY KEY (
    customer_id
  ) NOT ENFORCED;
}
