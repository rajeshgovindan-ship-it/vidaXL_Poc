-- Generated by vidaXL Fabric Accelerator (Dataform edition)
-- Target: gcp-accelerator-380712.gold.tc5_orders_volume
-- Tags: datamart_sales, tc5_orders_volume, gold, daily
-- Idempotent via ${ref()} + Dataform incremental/operations semantics
config {
  type: "table",
  schema: "gold",
  tags: ["datamart_sales", "tc5_orders_volume", "gold", "daily"],
  columns: {
    "order_id": "Order id.",
    "order_date": "Order date, parsed to DATE. -- Calculation: PARSE_DATE('%Y-%m-%d', order_date) AS order_date",
    "amount": "Order amount."
  },
  bigquery: {
    partitionBy: "DATE(order_date)",
  },
  description: "TC5: volume."
}

SELECT
  orders_data.order_id AS order_id,
  PARSE_DATE('%Y-%m-%d', order_date) AS order_date,
  orders_data.amount AS amount
FROM ${ref("orders_data")} AS orders_data
