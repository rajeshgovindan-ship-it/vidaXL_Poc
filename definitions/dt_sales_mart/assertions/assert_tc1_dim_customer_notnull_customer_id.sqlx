config {
  type: "assertion",
  name: "assert_tc1_dim_customer_notnull_customer_id",
  schema: "gold_assertions",
  description: "Non-null check for tc1_dim_customer.customer_id -- all data quality checks are generated as standalone assertion files."
}

SELECT *
FROM ${ref("tc1_dim_customer")}
WHERE customer_id IS NULL
