config {
  type: "assertion",
  name: "assert_tc1_dim_customer_notnull_dw_batch_date",
  schema: "gold_assertions",
  description: "Non-null check for tc1_dim_customer.dw_batch_date -- all data quality checks are generated as standalone assertion files."
}

SELECT *
FROM ${ref("tc1_dim_customer")}
WHERE dw_batch_date IS NULL
