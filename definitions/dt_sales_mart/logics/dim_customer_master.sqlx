-- Generated by vidaXL Fabric Accelerator (Dataform edition)
-- Target: gcp-accelerator-380712.gold.dim_customer_master
-- Tags: dt_sales_mart, dim_customer_master, gold, daily
-- Idempotent via ${ref()} + Dataform incremental/operations semantics
config {
  type: "table",
  schema: "gold",
  tags: ["dt_sales_mart", "dim_customer_master", "gold", "daily"],
  columns: {
    "customer_id": "TODO: add a description for this column.",
    "customer_name": "TODO: add a description for this column."
  },
  description: "UNION DISTINCT: merges customer_data_v1 + customer_data_with_tags, deduped by id."
}

WITH customers_unioned AS (
  SELECT
    customer_data_v1.customer_id AS customer_id,
    customer_data_v1.customer_name AS customer_name
  FROM ${ref("customer_data_v1")} AS customer_data_v1
  UNION DISTINCT
  SELECT
    SAFE_CAST(Customer_Id AS INT64) AS customer_id,
    customer_data_with_tags.First_Name AS customer_name
  FROM ${ref("customer_data_with_tags")} AS customer_data_with_tags
)
SELECT
  customers_unioned.customer_id AS customer_id,
  customers_unioned.customer_name AS customer_name
FROM customers_unioned AS customers_unioned


post_operations {
  ALTER TABLE ${self()}
  ADD PRIMARY KEY (
    customer_id
  ) NOT ENFORCED;
}
