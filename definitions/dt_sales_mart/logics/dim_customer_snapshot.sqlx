-- Generated by vidaXL Fabric Accelerator (Dataform edition)
-- Target: gcp-accelerator-380712.gold.dim_customer_snapshot
-- Tags: dt_sales_mart, dim_customer_snapshot, gold, daily
-- Idempotent via ${ref()} + Dataform incremental/operations semantics
config {
  type: "table",
  schema: "gold",
  tags: ["dt_sales_mart", "dim_customer_snapshot", "gold", "daily"],
  columns: {
    "customer_id": "Customer id.",
    "customer_name": "Name."
  },
  description: "TABLE: full rebuild every run -- simplest, but rewrites the whole table each time."
}

SELECT
  customer_data_v1.customer_id AS customer_id,
  customer_data_v1.customer_name AS customer_name
FROM ${ref("customer_data_v1")} AS customer_data_v1


post_operations {
  ALTER TABLE ${self()}
  ADD PRIMARY KEY (
    customer_id
  ) NOT ENFORCED;
}
