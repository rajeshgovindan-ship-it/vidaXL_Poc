-- Generated by vidaXL Fabric Accelerator (Dataform edition)
-- Target: gcp-accelerator-380712.gold.vw_active_customers
-- Tags: dt_sales_mart, vw_active_customers, gold, daily
-- Idempotent via ${ref()} + Dataform incremental/operations semantics
config {
  type: "view",
  schema: "gold",
  tags: ["dt_sales_mart", "vw_active_customers", "gold", "daily"],
  columns: {
    "customer_id": "Customer id.",
    "account_balance": "Balance, view-filtered to active customers only."
  },
  description: "VIEW: live, always up to date, no storage cost -- but recomputed on every query."
}

SELECT
  customer_data_v1.customer_id AS customer_id,
  customer_data_v1.account_balance AS account_balance
FROM ${ref("customer_data_v1")} AS customer_data_v1
WHERE account_balance > 0
