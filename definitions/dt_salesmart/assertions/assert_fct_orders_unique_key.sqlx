config {
  type: "assertion",
  name: "assert_fct_orders_unique_key",
  description: "Uniqueness check for fct_orders (order_id) -- all data quality checks are generated as standalone assertion files."
}

SELECT order_id, COUNT(*) AS row_count
FROM ${ref("fct_orders")}
GROUP BY order_id
HAVING COUNT(*) > 1
