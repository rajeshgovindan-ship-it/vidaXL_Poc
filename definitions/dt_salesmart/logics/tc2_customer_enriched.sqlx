-- Generated by vidaXL Fabric Accelerator (Dataform edition)
-- Target: gcp-accelerator-380712.gold.tc2_customer_enriched
-- Tags: dt_salesmart, tc2_customer_enriched, gold, daily
-- Idempotent via ${ref()} + Dataform incremental/operations semantics
config {
  type: "table",
  schema: "gold",
  tags: ["dt_salesmart", "tc2_customer_enriched", "gold", "daily"],
  columns: {
    customer_id: "Unique id.",
    customer_name: "Name.",
    signup_timestamp: "Signup ts.",
    country: "Country.",
    city: "City."
  },
  bigquery: {
    partitionBy: "DATE(signup_timestamp)",
    clusterBy: ["customer_id"],
  },
  description: "TC2 INNER JOIN."
}

SELECT
  customer_data_v1.customer_id AS customer_id,
  customer_data_v1.customer_name AS customer_name,
  customer_data_v1.signup_timestamp AS signup_timestamp,
  customer_data_with_tags.Country AS country,
  customer_data_with_tags.City AS city
FROM ${ref("customer_data_v1")} AS customer_data_v1
INNER JOIN ${ref("customer_data_with_tags")} AS customer_data_with_tags ON CAST(customer_data_v1.customer_id AS STRING) = customer_data_with_tags.Customer_Id


post_operations {
  ALTER TABLE ${self()}
  ADD PRIMARY KEY (
    customer_id
  ) NOT ENFORCED;
}
