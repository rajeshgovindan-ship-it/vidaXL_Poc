-- Generated by Tiger Data Fabric (Dataform edition)
-- Target: gcp-accelerator-380712.gold.Retail_raw_refined
-- Tags: 
-- Idempotent via ${ref()} + Dataform incremental/operations semantics
config {
  type: "table",
  schema: "gold",
  tags: [],
  bigquery: {
    partitionBy: "InvoiceDate",
    clusterBy: ["CustomerID"],
  },
  assertions: {
    uniqueKey: ["InvoiceNo", "StockCode", "CustomerID"]
  },
  description: "Gold table 'Retail_raw_refined' -- full snapshot, rebuilt on every run."
}

WITH retail_sales AS (
  SELECT
    Retail_raw.InvoiceNo AS InvoiceNo
  FROM ${ref("Retail_raw")} AS Retail_raw
)
SELECT
  Retail_raw.StockCode AS StockCode,
  Retail_raw.Description AS Description,
  Retail_raw.Quantity AS Quantity,
  Retail_raw.InvoiceDate AS InvoiceDate,
  Retail_raw.UnitPrice AS UnitPrice,
  Retail_raw.CustomerID AS CustomerID,
  Retail_raw.Country AS Country
FROM ${ref("Retail_raw")} AS Retail_raw


post_operations {
  ALTER TABLE ${self()}
  ADD PRIMARY KEY (
    InvoiceNo,
    StockCode,
    CustomerID
  ) NOT ENFORCED;
}
