-- Generated by Tiger Data Fabric (Dataform edition)
-- Target: gcp-accelerator-380712.gold.customer_data_with_tags_refined
-- Tags: 
-- Idempotent via ${ref()} + Dataform incremental/operations semantics
config {
  type: "table",
  schema: "gold",
  tags: [],
  bigquery: {
    partitionBy: "Subscription_Date",
    clusterBy: ["Customer_Id"],
  },
  assertions: {
    uniqueKey: ["Customer_Id", "Country", "Subscription_Date"]
  },
  description: "Gold table 'customer_data_with_tags_refined' -- full snapshot, rebuilt on every run."
}

WITH customer_info AS (
  SELECT
    customer_data_with_tags.Index AS Index
  FROM ${ref("customer_data_with_tags")} AS customer_data_with_tags
)
SELECT
  customer_data_with_tags.Customer_Id AS Customer_Id,
  customer_data_with_tags.First_Name AS First_Name,
  customer_data_with_tags.Last_Name AS Last_Name,
  customer_data_with_tags.Company AS Company,
  customer_data_with_tags.City AS City,
  customer_data_with_tags.Country AS Country,
  customer_data_with_tags.Phone_1 AS Phone_1,
  customer_data_with_tags.Phone_2 AS Phone_2,
  customer_data_with_tags.Email AS Email,
  customer_data_with_tags.Subscription_Date AS Subscription_Date,
  customer_data_with_tags.Website AS Website
FROM ${ref("customer_data_with_tags")} AS customer_data_with_tags


post_operations {
  ALTER TABLE ${self()}
  ADD PRIMARY KEY (
    Customer_Id,
    Country,
    Subscription_Date
  ) NOT ENFORCED;
}
